---
title: Test the different knitr engines
---

```{r setup}
library(roxyblox)
library(knitr)
```

```{r some-code}
x  <- "The cat purrrs on my lap."
```

```{carp test-1}
#' @challenge Count words
#' 
#' 1. How many words are in the sentence `x`?
#'
#' @solution
#'
#' There is more than one way of doing this but here is a solution.
#' 
#' ```{r test-1-solution-1}
#' n_words <- length(strsplit(x, " ")[[1]])
#' n_words
#' ```
#'
#' 1. How many letters are in the sentence `x`?
#' 
#'
#' ```{r test-1-solution-2}
#' n_chars <- nchar(gsub("[^a-zA-Z]", "", x))
#' n_chars
#' ```
```

Now that we know that there are `r n_words` and `r n_chars` in the sentence. Let's add these 2 numbers

```{carp test-2}
#' @challenge add these 2 numbers
#' 
#' @solution
#' Are these variables available?
#' 
#' ```{r test-2-solution}
#' n_words + n_chars
#' ```
```

